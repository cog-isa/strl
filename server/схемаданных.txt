моделирование
   f_ID
   f_Дата
   f_Имя

   .запуски (наследовать модель) каждый раз по кнопке *запуск* генерить новый запуск
	   .настройка
		<?>
		масштаб карты, 
		дискретный шаг, 
 		тип моделир:
		  (1-off.video (перемотка)
		   2-off.visual (редактируем) - придумать замену одному из offline
		   3-online)

	   .логи (ЧТО ЕСТЬ ЛОГИ?)	
		пишем в базу время с дискретным шагом, заданным в настройках

		время t1:  (как это представить в реляц. бд?)
			объект1 ()
			объект2 ()
			...

		время t2:
			{объект1 не изменился поэтому не будет занесен сюда} - хрень наверно. проще все писать.. медленно но надежно, проблем в логике не будет
			объект2 ()
			объект3 ()
			...

		если перезатирать данные, то надо знать до какого момента откатываться(хранить counter таблицы)

		<?>""" у каждой переменной (свойства/поля объекта) есть история изменений., есть события."""
	
		<?> копировать (срезы) без ссылок


		если все не перечислять в логах, то каждый раз придется джойнить таблицы.. 
		но тогда теряются свойства (Жизнь, проходимость, объем топлива на данный момент)

		поля:	ID, ID объекта, ID запуска, время, свойства(масштаб в свойствах)


   .объект
	.свойства
	   .Форма - круг или многоугольник (изменение формы объектов в процессе моделирования не предусмотрено) и местоположение объекта на карте
	   .Активность - активный и пассивный
	   .Проходимость – 0, .. 100
	   .Жизнь: 0,…,100. При 0 объект исчезает
	   .Материал(перечень) список
	   .температура C
	   .отражательная способность %
	   .объем топлива  %
				""система свойств объекта должна быть расширяемой на программном уровне
				Изменение перечня свойств в процессе моделирования не предусмотрено""
	.тип - string
	.датчики - []
	.координата_x
	.координата_y
	

	